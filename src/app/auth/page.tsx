import LoginFormCard from "@/components/LoginFormCard";

const page = () => {
  return (
    <section className="grid h-dvh place-items-center">
      <LoginFormCard />
    </section>
  );
};

export default page;
