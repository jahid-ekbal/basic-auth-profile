import UserProfile from "@/components/UserProfile";

const page = () => {
  return (
    <section className="grid h-dvh place-items-center gap-4">
      <UserProfile />
    </section>
  );
};

export default page;
